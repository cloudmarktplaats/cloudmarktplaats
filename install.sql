ALTER TABLE products ADD COLUMN IF NOT EXISTS description TEXT AFTER specs,
ADD COLUMN IF NOT EXISTS featured TINYINT(1) DEFAULT 0 AFTER description,
ADD COLUMN IF NOT EXISTS is_new TINYINT(1) DEFAULT 0 AFTER featured,
ADD COLUMN IF NOT EXISTS image VARCHAR(255) AFTER is_new,
ADD COLUMN IF NOT EXISTS state VARCHAR(50) AFTER category,
ADD COLUMN IF NOT EXISTS specs TEXT AFTER state,
ADD COLUMN IF NOT EXISTS user_id INT AFTER id,
ADD COLUMN IF NOT EXISTS category VARCHAR(100) AFTER name,
ADD COLUMN IF NOT EXISTS price DECIMAL(10,2) AFTER state,
ADD COLUMN IF NOT EXISTS created_at DATETIME DEFAULT CURRENT_TIMESTAMP AFTER price; 